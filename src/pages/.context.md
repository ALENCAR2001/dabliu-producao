<context path="src/pages" updated="2026-05-25">

Páginas (views) renderizadas pelas rotas definidas em `src/App.tsx`. Cada arquivo exporta um componente React default para uma seção do sistema.

## Summary

- Dashboard.tsx — visão geral (cards/indicadores estáticos por enquanto).
- Layouts.tsx — CRUD em memória de layouts (lista + modal de cadastro).
- Producao.tsx — tela de contagem/registro de produção (UI estática, sem estado persistente).
- Administracao.tsx — tela de administração/configurações (UI estática).

## Constraints

- MUST manter exports default (usado diretamente no roteamento em `src/App.tsx`).

</context>
